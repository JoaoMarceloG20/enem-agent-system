# TASK: Implementar BaseENEMAgent

## Objetivo
Criar a classe abstrata BaseENEMAgent seguindo exatamente os padrões do dr_ubyfol, servindo como base para todos os 6 agentes ENEM (TutorAgent, QuizAgent, EssayGraderAgent, StudyPlanAgent, OrchestratorAgent).

## Contexto Completo

### Padrão de Referência (dr_ubyfol)
```python
def get_dr_ubyfol_agent(
    model_id: str = 'gemini-2.5-flash',
    user_id: Optional[str] = None,
    session_id: Optional[str] = None,
    debug_mode: bool = True,
) -> Agent:
    return Agent(
        name='Dr. Ubyfol',
        agent_id='dr_ubyfol',
        user_id=user_id,
        session_id=session_id,
        model=Gemini(id=model_id, api_key=settings.GOOGLE_API_KEY),
        tools=[ReasoningTools(add_instructions=True)],
        description=dedent("..."),
        instructions=dedent("..."),
        add_state_in_messages=True,
        knowledge=get_dr_ubyfol_knowledge(),
        search_knowledge=True,
        storage=PostgresAgentStorage(
            table_name='agno_assist_sessions',
            db_url=settings.SQLALCHEMY_DATABASE_URI,
        ),
        add_history_to_messages=True,
        num_history_runs=3,
        read_chat_history=True,
        memory=Memory(
            model=Gemini(id=model_id, api_key=settings.GOOGLE_API_KEY),
            db=PostgresMemoryDb(
                table_name='user_memories',
                db_url=settings.SQLALCHEMY_DATABASE_URI,
            ),
            delete_memories=True,
            clear_memories=True,
        ),
        enable_agentic_memory=True,
        markdown=True,
        add_datetime_to_instructions=True,
        debug_mode=debug_mode,
    )
```

### Estrutura para Agentes ENEM
Baseado em AGENTES_MAPEAMENTO.md, temos 6 agentes principais:
1. **BaseENEMAgent** - Classe abstrata (esta task)
2. **TutorAgent** - Tutor educacional 
3. **QuizAgent** - Gerador de quiz
4. **EssayGraderAgent** - Corretor de redação
5. **StudyPlanAgent** - Planos de estudo
6. **OrchestratorAgent** - Coordenação central

## Requisitos Detalhados

### 1. Classe Abstrata BaseENEMAgent
- [ ] Importar todas as dependências do padrão dr_ubyfol
- [ ] Métodos abstratos que cada agente deve implementar
- [ ] Configurações comuns (PostgresAgentStorage, Memory, etc.)
- [ ] Sistema de logs e métricas integrado
- [ ] Health checks automatizados

### 2. Configurações Padrão ENEM
- [ ] Model: Gemini 2.5 Flash (mesmo do dr_ubyfol)
- [ ] Storage: PostgresAgentStorage compatível
- [ ] Memory: PostgresMemoryDb para personalização
- [ ] Knowledge: Sistema baseado em get_enem_knowledge_base()
- [ ] Tools: ReasoningTools + tools específicos por agente

### 3. Funcionalidades Base
- [ ] Session management (user_id, session_id)
- [ ] Chat history (3 últimas mensagens)
- [ ] Agentic memory para personalização
- [ ] Markdown response formatting
- [ ] Debug mode configurable
- [ ] Datetime injection in instructions

### 4. Sistema de Health Checks
- [ ] Verificar conexão PostgreSQL
- [ ] Verificar conexão Qdrant
- [ ] Verificar Google API Key
- [ ] Status de cada agente individual

### 5. Integration com Knowledge Base
- [ ] Usar get_enem_knowledge_base() do knowledge.py
- [ ] Search knowledge habilitado
- [ ] Collections Qdrant por matéria

## Critérios de Aceitação
- [ ] BaseENEMAgent implementado seguindo padrão dr_ubyfol
- [ ] Métodos abstratos definidos para agentes específicos
- [ ] Configurações PostgreSQL + Qdrant funcionais
- [ ] Health check system implementado
- [ ] Compatibilidade 100% com estrutura Agno
- [ ] Logs estruturados e métricas

## Estrutura de Implementação

### Arquivo: app/agents/base_enem_agent.py
```python
from abc import ABC, abstractmethod
from typing import Optional, Dict, Any
from agno.agent import Agent, AgentKnowledge
from agno.models.google import Gemini
from agno.storage.agent.postgres import PostgresAgentStorage
from agno.memory.v2.memory import Memory
from agno.memory.v2.db.postgres import PostgresMemoryDb
from agno.tools.reasoning import ReasoningTools

from app.core.config import settings
from app.agents.knowledge import get_enem_knowledge_base

class BaseENEMAgent(ABC):
    """Base class for all ENEM agents following dr_ubyfol pattern"""
    
    @abstractmethod
    def get_agent_type(self) -> str:
        """Return agent type identifier"""
        pass
    
    @abstractmethod
    def get_agent_description(self) -> str:
        """Return agent description"""
        pass
    
    @abstractmethod
    def get_agent_instructions(self) -> str:
        """Return agent instructions"""
        pass
    
    @abstractmethod
    def get_agent_tools(self) -> list:
        """Return agent-specific tools"""
        pass
    
    def get_agent(
        self,
        model_id: str = 'gemini-2.5-flash',
        user_id: Optional[str] = None,
        session_id: Optional[str] = None,
        debug_mode: bool = True,
    ) -> Agent:
        """Create agent instance following dr_ubyfol pattern"""
        # Implementation here...
```

### Métodos Abstratos Obrigatórios
Cada agente ENEM deve implementar:
- `get_agent_type()` - ID único do agente
- `get_agent_description()` - Descrição do agente
- `get_agent_instructions()` - Instruções específicas
- `get_agent_tools()` - Tools específicas do agente

### Health Check System
- `health_check()` - Verificar status do agente
- `check_dependencies()` - PostgreSQL, Qdrant, Google API
- `get_metrics()` - Métricas de uso e performance

## Referências
- **Padrão**: `dr_ubyfol.py` linhas 45-158
- **Knowledge**: `app/agents/knowledge.py`
- **Config**: `app/core/config.py`
- **Models**: `app/models.py` (tabelas compatíveis)

## Próximos Passos Após Conclusão
- **TASK 5**: Implementar TutorAgent usando BaseENEMAgent
- **TASK 6**: Implementar QuizAgent usando BaseENEMAgent
- **TASK 7**: Implementar EssayGraderAgent usando BaseENEMAgent
- **TASK 8**: Implementar StudyPlanAgent usando BaseENEMAgent
- **TASK 9**: Implementar OrchestratorAgent usando BaseENEMAgent

## Validação
- [ ] BaseENEMAgent instanciável via factory pattern
- [ ] Health checks funcionais
- [ ] Compatibilidade com PostgresAgentStorage
- [ ] Integration com sistema de knowledge ENEM
- [ ] Debug logs estruturados funcionais