# TASK: Setup da Estrutura Base do Projeto

## Objetivo
Copiar e adaptar a estrutura base do projeto ubyfol-platform/backend para criar a fundação do edtech-agent-api, mantendo compatibilidade total com os padrões estabelecidos.

## Contexto Completo

### Projeto de Referência
- **Path**: `/Users/pablofernando/work/ntropy/ubyfol/repos/ubyfol-platform/backend`
- **Padrão Ouro**: Agent dr_ubyfol como referência arquitetural
- **Tecnologias**: Agno + Google Gemini + FastAPI + PostgreSQL + Qdrant

### Estrutura Atual do Projeto de Referência
```
backend/
├── Dockerfile
├── README.md
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── models.py
│   ├── agents/
│   │   ├── dr_ubyfol.py (padrão ouro)
│   │   ├── finance_agent.py
│   │   ├── web_agent.py
│   │   └── data/pdfs/
│   ├── api/
│   │   ├── main.py
│   │   └── routes/
│   └── core/
│       ├── config.py
│       ├── db.py
│       └── qdrant.py
├── docker-compose.yml
├── example.env
├── pyproject.toml
├── scripts/
└── uv.lock
```

## Requisitos Detalhados

### 1. Cópia da Estrutura Base
- [ ] Copiar todos os arquivos de configuração (pyproject.toml, example.env)
- [ ] Copiar Dockerfile e docker-compose.yml
- [ ] Copiar scripts utilitários (dev.sh, format.sh, etc.)
- [ ] Copiar estrutura app/ completa
- [ ] Copiar app/core/ (config.py, db.py, qdrant.py)

### 2. Adaptações para Edtech
- [ ] Atualizar PROJECT_NAME em config.py para "Edtech Agent API"
- [ ] Adaptar docker-compose.yml para contexto edtech
- [ ] Atualizar pyproject.toml com nome e descrição adequados
- [ ] Criar .env.example específico para o projeto

### 3. Estrutura de Diretórios para Agentes ENEM
- [ ] Limpar app/agents/ mantendo apenas estrutura base
- [ ] Criar diretório app/agents/data/pdfs/ para materiais ENEM
- [ ] Preparar estrutura para os 6 agentes ENEM
- [ ] Manter compatibilidade com padrão dr_ubyfol

### 4. Configurações Específicas
- [ ] Adaptar settings para múltiplos agentes
- [ ] Configurar collections do Qdrant para conhecimento ENEM
- [ ] Preparar variáveis de ambiente para Google Gemini
- [ ] Configurar database para chat history e memory

## Critérios de Aceitação
- [ ] Estrutura base copiada e funcional
- [ ] Docker-compose rodando sem erros
- [ ] Configurações adaptadas para edtech context
- [ ] Compatibilidade total com padrões ubyfol-platform
- [ ] Health checks funcionando
- [ ] Preparação completa para implementação dos agentes ENEM

## Arquivos para Copiar
1. **Configuração**:
   - pyproject.toml → adaptar nome/descrição
   - example.env → renomear variáveis
   - docker-compose.yml → manter serviços (db, qdrant, backend)
   - Dockerfile → manter como está

2. **Scripts**:
   - scripts/ → copiar todos os scripts utilitários
   - dev.sh, format.sh, validate.sh

3. **App Core**:
   - app/core/config.py → adaptar PROJECT_NAME e variáveis
   - app/core/db.py → manter configuração PostgreSQL
   - app/core/qdrant.py → preparar para conhecimento ENEM
   - app/main.py → adaptar imports e configurações

4. **API Base**:
   - app/api/ → copiar estrutura completa
   - routes/health.py → manter health checks
   - routes/agents.py → preparar para múltiplos agentes

## Referências
- Arquivo de referência principal: `dr_ubyfol.py`
- Configurações: `app/core/config.py`
- Docker: `docker-compose.yml`
- Padrões de código: `.claude/memoria/padroes_arquitetura.md`

## Próximos Passos Após Conclusão
- TASK 3: Configurar banco de dados e serviços
- TASK 4: Implementar BaseENEMAgent
- Validar que estrutura está 100% compatível com ubyfol-platform